# Ready Or Not – Shout Key
THE README IS GENERATED BY AI I WERE TOO LAZY TO DO IT 

Desktop Electron + React app that listens to your microphone and, when you shout, sends a global keypress (default **F**) to trigger the in‑game shout / interaction in **Ready Or Not** (including VR mod).

No need to follow the tutorial - exe or zip file with everything you need pre-installed on release page


## Features

- Real‑time microphone monitoring with live level bar.
- Threshold‑based shout detection with configurable sensitivity.
- Configurable shout key (default `F`).
- Configurable cooldown between shouts (ms).
- Guided calibration view to auto‑set the threshold.
- Bottom **Dock** navigation (Home / Calibration / Settings) using the provided `motion` Dock component.
- Dark, game‑inspired UI.

## Requirements

- Windows 10/11
- Node.js and npm installed
- Microphone connected and accessible
- Ready Or Not running in the foreground (or any game / app that should receive the key)

## Install & run

```bash
npm install
npm start
```

`npm start` will:

1. Build the React frontend with Vite into `dist/`.
2. Launch Electron which loads the built UI and listens to your microphone.

> On first run, Windows / Electron will ask for microphone permissions. You **must** allow them.

### Dev (frontend only)

If you want to tweak the React UI without Electron:

```bash
npm run dev
```

This runs Vite’s dev server at `http://localhost:5173`, but **key simulation won’t work** in the browser – it only works through Electron.

## How it works

- The React frontend (renderer) uses the Web Audio API to:
  - Capture your microphone.
  - Compute a simple RMS level.
  - Display the current level (0–100) and the threshold.
  - When the level exceeds the threshold and cooldown has elapsed, it calls `window.electronAPI.triggerKey()`.

- The Electron backend (main process) listens for the `shout-keypress` IPC and uses **robotjs** to send a global key tap to Windows. The active window (typically Ready Or Not) receives the key.

## Changing the shout key / sensitivity

Go to **Settings**:

- **Shout key**: Single character like `F`, `E`, `G`… (case-insensitive, sent as lowercase to Windows).
- **Threshold**: 5–100 (internal, not real dB). Higher = less sensitive.
- **Cooldown**: Minimum time between shouts (ms).

Click **Save** to persist them (stored in `localStorage`).

Use **Restore defaults** to reset to:

- Key: `F`
- Threshold: `35`
- Cooldown: `1200` ms

## Notes

- This is not an anti‑cheat‑safe tool, just a simple accessibility / immersion helper. Use at your own risk on online servers.
- If `robotjs` fails to build, make sure you have:
  - Build tools for your Node version (e.g., `windows-build-tools` / Visual Studio Build Tools).
  - Python installed (many native Node modules require it).

## Folder structure

```text
ReadyOrNot-ShoutKeyApp/
  electron/
    main.js        # Electron main process – loads UI, handles keypress IPC
    preload.js     # Exposes window.electronAPI in the renderer
  src/
    App.jsx        # Core React app, audio logic, state
    main.jsx       # React entrypoint
    styles/
      global.css   # Global styling + layout
    components/
      Dock.jsx     # Provided Dock component (navigation)
      Dock.css     # Dock styles (from snippet)
    views/
      Home.jsx         # Live dashboard
      Calibration.jsx  # Threshold calibration
      Settings.jsx     # Key, threshold, cooldown settings
  index.html
  package.json
  vite.config.js
  README.md
```

## Start playing

1. Launch the app with `npm start`.
2. In the **Calibration** view, click **Start calibration**, speak normally, then shout as you would in‑game.
3. Adjust the key / cooldown in **Settings** if needed.
4. Start Ready Or Not (VR mod), focus the game window.
5. Shout in real life – the app will send `F` (or your chosen key) whenever you cross the threshold.

Voici un **README complet** (anglais + français dans le même fichier), avec la mention IA au tout début. Tu peux remplacer entièrement ton `README.md` par ce contenu :

````markdown
# Ready Or Not – Shout Key

> ⚠️ This README (English + Français) was generated with the help of an AI assistant to document the project.  
> The program itself is a small personal tool and should be used at your own risk.

---

## 🇬🇧 English version

Desktop Electron + React app that listens to your microphone and, when you shout, sends a global keypress (default **F**) to trigger the in-game shout / interaction in **Ready Or Not** (including VR mod).

---

### Features

- Real-time microphone monitoring with live level bar.
- Threshold-based shout detection with configurable sensitivity.
- Configurable shout key (default `F`).
- Configurable cooldown between shouts (ms).
- Guided calibration view to auto-set the threshold.
- Bottom **Dock** navigation (Home / Calibration / Settings) using the provided `motion` Dock component.
- Dark, game-inspired UI.

---

### Requirements

- Windows 10/11
- Node.js and npm installed
- Microphone connected and accessible
- Ready Or Not running in the foreground (or any game / app that should receive the key)

---

### Install & run

```bash
npm install
npm start
````

`npm start` will:

1. Build the React frontend with Vite into `dist/`.
2. Launch Electron which loads the built UI and listens to your microphone.

> On first run, Windows / Electron will ask for microphone permissions. You **must** allow them.

---

### Dev (frontend only)

If you want to tweak the React UI without Electron:

```bash
npm run dev
```

This runs Vite’s dev server at `http://localhost:5173`, but **key simulation won’t work** in the browser – it only works through Electron.

---

### How it works

* The React frontend (renderer) uses the Web Audio API to:

  * Capture your microphone.
  * Compute a simple RMS level.
  * Display the current level (0–100) and the threshold.
  * When the level exceeds the threshold and cooldown has elapsed, it calls `window.electronAPI.triggerKey()`.

* The Electron backend (main process) listens for the `shout-keypress` IPC and uses **robotjs** (or an equivalent key-simulation method) to send a global key tap to Windows.
  The active window (typically Ready Or Not) receives the key.

---

### Changing the shout key / sensitivity

Go to **Settings**:

* **Shout key**: Single character like `F`, `E`, `G`… (case-insensitive, sent as lowercase to Windows).
* **Threshold**: 5–100 (internal, not real dB). Higher = less sensitive.
* **Cooldown**: Minimum time between shouts (ms).

Click **Save** to persist them (stored in `localStorage`).

Use **Restore defaults** to reset to:

* Key: `F`
* Threshold: `35`
* Cooldown: `1200` ms

---

### Notes

* This is not an anti-cheat-safe tool, just a simple accessibility / immersion helper. Use at your own risk on online servers.
* If `robotjs` (or any native module) fails to build, make sure you have:

  * Build tools for your Node version (e.g., Visual Studio Build Tools / `windows-build-tools`).
  * Python installed (many native Node modules require it).

---

### Folder structure

```text
ReadyOrNot-ShoutKeyApp/
  electron/
    main.js        # Electron main process – loads UI, handles keypress IPC
    preload.js     # Exposes window.electronAPI in the renderer
  src/
    App.jsx        # Core React app, audio logic, state
    main.jsx       # React entrypoint
    styles/
      global.css   # Global styling + layout
    components/
      Dock.jsx     # Provided Dock component (navigation)
      Dock.css     # Dock styles (from snippet)
    views/
      Home.jsx         # Live dashboard
      Calibration.jsx  # Threshold calibration
      Settings.jsx     # Key, threshold, cooldown settings
  index.html
  package.json
  vite.config.js
  README.md
```

---

### Start playing

1. Launch the app with `npm start`.
2. In the **Calibration** view, click **Start calibration**, speak normally, then shout as you would in-game.
3. Adjust the key / cooldown in **Settings** if needed.
4. Start Ready Or Not (VR mod), focus the game window.
5. Shout in real life – the app will send `F` (or your chosen key) whenever you cross the threshold.

---

## 🇫🇷 Version française

Application de bureau **Electron + React** qui écoute ton microphone et, quand tu cries, envoie une touche clavier globale (par défaut **F**) pour déclencher le cri / l’interaction en jeu dans **Ready Or Not** (y compris avec le mod VR).

---

### Fonctionnalités

* Surveillance du micro en temps réel avec barre de niveau.
* Détection de cri basée sur un **seuil** de volume configurable.
* Touche de cri configurable (par défaut `F`).
* Cooldown configurable entre deux cris (en millisecondes).
* Vue de **calibration** guidée pour ajuster automatiquement le seuil.
* Navigation par **Dock** en bas (Accueil / Calibration / Paramètres) avec le composant `motion` fourni.
* Interface sombre, inspirée des jeux, minimaliste.

---

### Prérequis

* Windows 10/11
* Node.js et npm installés
* Un microphone connecté et accessible par le système
* Ready Or Not lancé au premier plan (ou n’importe quel autre jeu / appli qui doit recevoir la touche)

---

### Installation & lancement

```bash
npm install
npm start
```

`npm start` fait :

1. Build du frontend React avec Vite dans `dist/`.
2. Lancement d’Electron, qui charge l’UI compilée et commence à écouter ton micro.

> Au premier lancement, Windows / Electron va demander l’autorisation d’utiliser le micro.
> Tu dois **accepter** pour que l’appli fonctionne.

---

### Mode dev (frontend uniquement)

Pour modifier l’UI React sans lancer Electron :

```bash
npm run dev
```

Cela démarre le serveur Vite sur `http://localhost:5173`, mais **la simulation de touche ne fonctionne pas dans le navigateur** – uniquement via Electron.

---

### Fonctionnement interne

* Le frontend React (renderer) utilise la Web Audio API pour :

  * Récupérer le flux du microphone.
  * Calculer un niveau RMS simple.
  * Afficher le niveau courant (0–100) et le seuil.
  * Quand le niveau dépasse le seuil et que le cooldown est écoulé, il appelle `window.electronAPI.triggerKey()`.

* Le backend Electron (processus principal) écoute l’IPC `shout-keypress` et utilise **robotjs** (ou une méthode équivalente) pour envoyer une pression de touche globale à Windows.
  La fenêtre active (normalement Ready Or Not) reçoit cette touche.

---

### Changer la touche de cri / la sensibilité

Dans l’onglet **Settings** (Paramètres) :

* **Shout key** : une seule touche, par exemple `F`, `E`, `G`… (insensible à la casse, envoyée en minuscule à Windows).
* **Threshold** (seuil) : 5–100 (échelle interne, pas des dB réels). Plus la valeur est **élevée**, moins c’est sensible.
* **Cooldown** : temps minimum entre deux cris (en millisecondes).

Clique sur **Save** pour enregistrer (les valeurs sont stockées dans `localStorage`).

Le bouton **Restore defaults** remet :

* Touche : `F`
* Seuil : `35`
* Cooldown : `1200` ms

---

### Remarques

* Cet outil n’est **pas** prévu pour contourner un anti-cheat. C’est juste un petit helper d’accessibilité / immersion. Utilisation à tes risques sur les serveurs en ligne.
* Si `robotjs` (ou un autre module natif) refuse de se compiler, vérifie que tu as :

  * Les outils de build adaptés à ta version de Node (par ex. Visual Studio Build Tools / `windows-build-tools`).
  * Python installé (beaucoup de modules Node natifs en ont besoin).

---

### Arborescence du projet

```text
ReadyOrNot-ShoutKeyApp/
  electron/
    main.js        # Processus principal Electron – charge l’UI, gère l’IPC de touche
    preload.js     # Expose window.electronAPI au renderer
  src/
    App.jsx        # App React principale, logique audio, état global
    main.jsx       # Point d’entrée React
    styles/
      global.css   # Styles globaux + layout
    components/
      Dock.jsx     # Composant Dock (navigation)
      Dock.css     # Styles du Dock (depuis le snippet)
    views/
      Home.jsx         # Tableau de bord en temps réel
      Calibration.jsx  # Calibration automatique du seuil
      Settings.jsx     # Touche, seuil, cooldown et autres paramètres
  index.html
  package.json
  vite.config.js
  README.md
```

---

### Comment jouer avec

1. Lance l’appli avec `npm start`.
2. Dans la vue **Calibration**, clique sur **Start calibration**, parle normalement puis crie comme tu le ferais en jeu.
3. Ajuste la touche et le cooldown dans **Settings** si besoin.
4. Lance Ready Or Not (mod VR), mets la fenêtre du jeu au premier plan.
5. Crie IRL – l’appli enverra la touche `F` (ou celle que tu as choisie) dès que tu dépasses le seuil configuré.

---

